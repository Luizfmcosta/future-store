/** Demo default when no query is typed — aligned with PLP bootstrap in `SearchPageContent`. */
export const DEFAULT_SEARCH_QUERY =
  "Wireless speaker for ~3 m living room, best value, up to R$ 5000";
